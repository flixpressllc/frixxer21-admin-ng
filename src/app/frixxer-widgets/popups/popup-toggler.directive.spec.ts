import { PopupTogglerDirective } from './popup-toggler.directive';

describe('PopupTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
